components:
  schemas:
    # フォローする側はリクエストではなく JWT の currentUser から決まるため
    # current_user_id は受け取らない
    FollowReauestData:
      type: object
      required:
        - followed_user_id
      properties:
        followed_user_id:
          type: integer
