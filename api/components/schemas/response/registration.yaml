components:
  schemas:
    SignUpResponseData:
      type: object
      required:
        - user
        - login_status
        - token
      properties:
        user:
          $reg: "./user.yaml#/components/schemas/UserResponseData"
        login_status:
          type: boolean
        token:
          type: string