components:
  schemas:
    SignUpResponseData:
      type: object
      required:
        - user
        - login_status
        - token
      properties:
        user:
          $ref: "./user.yaml#/components/schemas/UserResponseData"
        login_status:
          type: boolean
        token:
          type: string